# ── MongoDB ───────────────────────────────────────────────────────────────────
TLS_MONGO_URL=mongodb://mongodb:27017
TLS_MONGO_DB=tls

# ── API keys ──────────────────────────────────────────────────────────────────
TLS_API_KEY=sk-tls-changeme
TLS_ADMIN_API_KEY=sk-tls-admin-changeme

# ── Token limit policy ────────────────────────────────────────────────────────
TLS_TOKEN_LIMIT=50000
TLS_RESET_HOURS=5
