apiVersion: v1
kind: Service
metadata:
  name: tls-service
  labels:
    app: tls-service
spec:
  selector:
    app: tls-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
